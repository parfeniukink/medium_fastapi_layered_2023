"""
This package encapsulates all infrastructure logical components.

For example application infrastructure components corresponds to provide
software dependencies to run.

On the other hand, database infrastructure component corresponds to provide
database access layer interface to interact with.

All of components HAVE NOT to be dependent on other project components.
Instead, all of them have to be dependent on infrastructure package.
"""
